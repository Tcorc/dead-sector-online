services:
  - type: web
    name: dead-sector-reborn
    runtime: node
    rootDir: DeadSectorWeb_Reborn
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
